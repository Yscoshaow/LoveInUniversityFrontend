<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/config_item_bg"
    android:padding="14dp"
    android:layout_marginBottom="8dp">

    <!-- Color indicator -->
    <ImageView
        android:id="@+id/config_indicator"
        android:layout_width="4dp"
        android:layout_height="40dp"
        android:src="@drawable/indicator_purple"
        android:scaleType="fitXY"
        android:layout_marginEnd="12dp"
        android:contentDescription="" />

    <!-- Info -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/config_wearer_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#1E293B"
            android:textSize="15sp"
            android:fontFamily="sans-serif-medium" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="4dp">

            <TextView
                android:id="@+id/config_lock_type"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#64748B"
                android:textSize="12sp" />

            <TextView
                android:id="@+id/config_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                android:layout_marginStart="8dp" />

            <TextView
                android:id="@+id/config_permission"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="11sp"
                android:layout_marginStart="8dp"
                android:textColor="#8B5CF6" />
        </LinearLayout>
    </LinearLayout>

    <!-- Remaining time -->
    <TextView
        android:id="@+id/config_remaining"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#3B82F6"
        android:textSize="13sp"
        android:fontFamily="sans-serif-bold" />
</LinearLayout>
